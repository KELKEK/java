package com.shinhan.week3.day2;

public class RunnableExecuteService {

	public static void main(String[] args) {
/*		// TODO Auto-generated method stub
		String[][] mails = new String[1000][3];
		for(String[] arr : mails) {
			arr[0] = "admin@my.com";
			arr[1]  = "member" + i + "@My.com";
			arr[2]  = "신상품 입고되었습니다.";
			i ++;
		}
		
		//MultiThread(5개)로 1000명의 이메일을 보내기
		ExecutorService service = Executors.newFixedThreadPool(5);
		
		for(int index=0; index < 1000 index++) {
			int seq = index;
			service.execute(new Runnable(){//로컬클래스
				
				public void run() {
					String message =
							"쓰레드 이름 : " + Thread.currentThread().getName()
							+ "\nfrom " + mails[seq][0]
							+"\nTo : ", mails[seq][1]
							+"\n내용" + mails[seq][2]	
				}
			});
			
			
		}*/
	} 

}
